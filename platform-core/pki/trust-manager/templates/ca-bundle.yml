---
apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: global-ca-bundle
spec:
  sources:
    - secret:
        name: "cluster-ca"
        key: "ca.crt"
  target:
    configMap:
      key: "ca-bundle.crt"

